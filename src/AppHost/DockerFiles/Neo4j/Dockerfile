FROM neo4j:5.19

# Expor as portas do Neo4j (Web UI e Bolt protocol)
EXPOSE 7474 7687

# O entrypoint padrão já inicia o Neo4j com a configuração padrão
# Se quiser customizar config futura, podemos sobrescrever o comando

# Como o Aspire controla o container, o CMD original do neo4j já funciona
